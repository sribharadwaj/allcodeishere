package ip.geeks;

import java.util.Scanner;

public class ArrayOps {

	
	/*
	 * Given an array A of size N, your task is to do some operations, i.e., search
	 * an element, insert an element, and delete an element by completing the
	 * functions. Also, all functions should return a boolean value.
	 * 
	 * Input Format: The first line of input consists of T, the number of test
	 * cases. T testcases follow. Each testcase contains 3 lines of input. The first
	 * line of every test case consists of an integer N, denotes the number of
	 * elements in an array. The second line of every test cases consists of N
	 * spaced integers Ai. The third line of every test case consists of four
	 * integers X, Y, Yi and Z, denoting the elements for the search operation,
	 * insert operation, insert operation element index and delete operation
	 * respectively.
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		Scanner in = new Scanner(System.in);
		int numOfElems = in.nextInt();
		//int numbers = in.nextLine();
	}

}
